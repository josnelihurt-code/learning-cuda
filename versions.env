# Single source of truth for toolchain versions used by Docker builds.
# Dockerfiles declare the ARGs without defaults; every builder (scripts,
# CI) sources this file and passes them as --build-arg. Update here only.
GO_VERSION=1.27
GO_RELEASE=1.27.1
ALPINE_VERSION=3.24
